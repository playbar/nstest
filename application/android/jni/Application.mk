
#APP_CPPFLAGS += -fexceptions

#APP_ABI := armeabi mips armeabi-v7a
APP_PLATFORM := android-9

#APP_STL := stlport_static
#APP_STL := stlport_shared

APP_MODULES := libGGlibrary
APP_MODULES += bw22
APP_MODULES += bw23
APP_MODULES += libBugly
#APP_MODULES += libmm1
#APP_MODULES += libmm2
#APP_MODULES += paypalm_app_plugin_jar_360game
#APP_MODULES += FreeSky
#APP_MODULES += usedes


